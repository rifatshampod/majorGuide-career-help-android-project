<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fsktm.um.edu.mymajor">

    <application
        android:allowBackup="true"
        android:icon="@drawable/fav"
        android:label="@string/app_name"
        android:roundIcon="@drawable/fav"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".bottomNavigationActivity"></activity>
        <activity android:name=".ResumeShow" />
        <activity android:name=".Resume" />
        <activity android:name=".ForgetPass" />
        <activity android:name=".Register" />
        <activity android:name=".HomePage" />
        <activity android:name=".QuestionsActivity" />
        <activity android:name=".SetsActivity" />
        <activity android:name=".Category" />
        <activity android:name=".ScoreActivity" />
        <activity
            android:name=".splash"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>